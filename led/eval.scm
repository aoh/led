(define-library (led eval)
   
   (export
      led-eval) ;; buff command-tuple → buff' 
   
   (import
      (owl base))
   
   (begin
      
      (define (led-eval buff command)
         buff)))

