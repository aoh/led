#!/bin/bash

echo "Running tests"
for foo in *.io
do
	echo " - $foo"
	cat "$foo" | "$@" > /dev/null 2>&1
	cmp "$foo.out" "$foo.ok" || { echo "Test $foo failed: $(diff $foo.ok $foo.out)"; exit 1; }
	rm "$foo.out"
done

echo "Tests OK"
